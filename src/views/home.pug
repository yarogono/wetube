extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            .videosBlocks
                +videoBlock({
                    id: item.id,
                    title:item.title,
                    views:item.views,
                    videoFile:item.fileUrl,
                })
